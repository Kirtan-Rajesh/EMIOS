"""Pydantic v2 schemas (requests, responses, envelope) for the /api/v1/* surface.

Deliberately kept separate from the legacy ``app/models/schemas.py`` module, which
backs the existing unversioned ``/api/*`` routes and must not be touched.
"""
